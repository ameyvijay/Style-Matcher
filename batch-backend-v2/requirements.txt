# Antigravity Engine v2.0 — Dependencies
# Install: pip install -r requirements.txt

# ── Core API ──
fastapi
uvicorn[standard]

# ── Image Processing ──
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0
rawpy>=0.19

# ── ML Quality Assessment ──
pyiqa>=0.1.10
torch>=2.2
torchvision>=0.17

# ── Duplicate Detection ──
imagededup>=0.3

# ── Metadata ──
# exiftool: install via `brew install exiftool` (CLI tool, not pip)

# ── macOS Finder Tags ──
pyobjc-framework-Cocoa>=10.0

# ── Cloud Bridge ──
firebase-admin

# ── Testing ──
pytest
httpx
pytest-asyncio
sqlalchemy
psutil
